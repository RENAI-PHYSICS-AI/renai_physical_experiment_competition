# PowerPoint 配音视频制作与字幕添加方法

本文记录本仓库“声速测量实验智能助教”和“李萨如图形实验智能助教”两份答辩 PPT 已经实际采用并验证的自动配音、视频导出和硬字幕制作流程。

这里所说的“录制”不是实时屏幕录像，而是：**按页生成配音 → 把音频嵌入 PPT → 设置自动播放和自动翻页 → 由 Microsoft PowerPoint 渲染导出 MP4 → 使用 FFmpeg 烧录字幕**。这种方式不会录入鼠标、通知、窗口边框或环境噪声，时间轴也更容易复现。

## 1. 核心结论

- 讲稿按幻灯片拆分，每页生成一段 WAV，同时生成统一时间轴 `segments.json` 和 SRT。
- 声速项目使用中文女声 `zh-CN-XiaoxiaoNeural`；李萨如项目使用中文男声 `zh-CN-YunyangNeural`，两份报告的音色明确区分。
- 每个完整句或语义分句单独设置语速、音高、音量和停顿，比整篇使用一个语速更自然，也更容易突出重点。
- PPT 中的音频必须嵌入文件、进入页面后自动播放；每页停留时间应当等于该页音频时长再加约 `0.55` 秒缓冲。
- 视频必须由桌面版 Microsoft PowerPoint 的 `CreateVideo` 导出。当前机器上的 WPS 虽然注册了类似 COM 接口，但实际测试不能可靠生成 MP4。
- 字幕烧录会重新编码画面，但音频使用 `-c:a copy` 原样复制，配音不会再次有损压缩。
- 正式 SRT 保留语义完整的句段；烧录前另建临时副本，把过长字幕细分为更短的单行提示，不直接破坏正式 SRT。
- 最终硬字幕已成为视频像素的一部分，播放端不需要 PowerPoint、Python、FFmpeg、字体或外部 SRT。

## 2. 完整流程

```mermaid
flowchart LR
    A["12页PPT与约10分钟讲稿"] --> B["按句和语义分段"]
    B --> C["Edge神经语音逐段合成"]
    C --> D["拼成12段WAV并加入停顿"]
    D --> E["生成segments.json与正式SRT"]
    E --> F["把每页WAV嵌入PPT"]
    F --> G["设置自动播放与自动翻页"]
    G --> H["Microsoft PowerPoint导出1080p MP4"]
    E --> I["复制并细分临时烧录字幕"]
    H --> J["FFmpeg/libass烧录字幕"]
    I --> J
    J --> K["音画、字幕、编码与抽帧验收"]
```

流程中的关键原则是：**配音、翻页和字幕必须共用同一条时间轴**。如果三者分别估算，页面切换后字幕会逐步漂移，或者配音尾句会被提前截断。

## 3. 输入、产物与当前成品

### 3.1 声速项目

| 类型 | 仓库相对路径 |
|---|---|
| 原始答辩 PPT | `声速/设计报告/声速测量实验智能助教_答辩PPT.pptx` |
| 逐页讲稿 | `声速/设计报告/声速测量实验智能助教_10分钟讲稿.txt` |
| 嵌入音频后的 PPT | `声速/设计报告/声速测量实验智能助教_自动配音版.pptx` |
| 正式 SRT | `声速/设计报告/声速测量实验智能助教_自动配音字幕.srt` |
| 最终硬字幕视频 | `声速/设计报告/声速测量实验智能助教_自动配音报告视频.mp4` |

### 3.2 李萨如项目

| 类型 | 仓库相对路径 |
|---|---|
| 原始答辩 PPT | `李萨如/设计报告/李萨如图形实验智能助教_答辩PPT.pptx` |
| 逐页讲稿 | `李萨如/设计报告/李萨如图形实验智能助教_10分钟讲稿.txt` |
| 嵌入音频后的 PPT | `李萨如/设计报告/李萨如图形实验智能助教_自动配音版.pptx` |
| 正式 SRT | `李萨如/设计报告/李萨如图形实验智能助教_自动配音字幕.srt` |
| 最终硬字幕视频 | `李萨如/设计报告/李萨如图形实验智能助教_自动配音报告视频.mp4` |

### 3.3 当前成品参数

| 项目 | 音色 | 12页音频 | PPT计时合计 | 视频时长 | 最终大小 | SHA-256 |
|---|---|---:|---:|---:|---:|---|
| 声速 | `zh-CN-XiaoxiaoNeural` 女声 | 618.130 秒 | 624.730 秒 | 624.925 秒 | 38,798,805 B | `7A6F92FD56FE097203AA9FA9C59A3D15E5C38C5333E4E4D98424CB30CF9F1ADE` |
| 李萨如 | `zh-CN-YunyangNeural` 男声 | 594.402 秒 | 601.002 秒 | 601.171 秒 | 38,336,494 B | `47CAD55C43279360CA259F11358210DEDFF37D057975DFDCDD46082C275F1F8B` |

两份最终视频均为 H.264 画面和 AAC 44.1 kHz 立体声音轨，帧率约 30 fps。

实际验收中分别从烧录前、烧录后视频抽取 AAC 码流并计算哈希，两边一致，证明 `-c:a copy` 没有改变配音。两份正式 SRT 去除人为换行后，也都与对应讲稿正文逐字一致。

## 4. 制作机与播放端依赖

| 组件 | 制作机 | 最终硬字幕 MP4 播放端 |
|---|---|---|
| Windows、PowerShell | 需要 | 不需要 |
| 桌面版 Microsoft PowerPoint | 嵌入音频和导出视频时需要 | 不需要 |
| Python | 生成配音、时间轴和 SRT 时需要 | 不需要 |
| `edge-tts` 与网络 | 合成语音时需要 | 不需要 |
| `imageio-ffmpeg` | MP3 解码、WAV 拼接和音频处理时使用 | 不需要 |
| `mutagen` | 李萨如脚本读取 MP3 时长时使用 | 不需要 |
| 带 `subtitles/libass` 和 `libx264` 的 FFmpeg | 烧录硬字幕时需要 | 不需要 |
| 微软雅黑等中文字体 | 字幕烧录时需要 | 硬字幕播放时不需要 |
| H.264/AAC 播放器 | 用于验收 | 需要 |
| Julia、Streamlit、RAG 或 API Key | 本流程不需要 | 不需要 |

`edge-tts` 使用网络语音服务，音色列表和服务行为未来可能变化。因此每次重新生成都应记录音色名称、依赖版本、讲稿哈希和输出音频哈希，不能只记录“女声”或“男声”。

### 4.1 检查 FFmpeg 是否具备字幕能力

不能仅凭“已经安装 FFmpeg”判断可用。必须确认构建中含有 `subtitles` 滤镜：

```powershell
& $ffmpegExe -hide_banner -filters | Select-String 'subtitles'
```

最终成片烧录时实际使用的是 `imageio-ffmpeg` 临时目录中的 FFmpeg 7.1 完整构建；该临时二进制已在缓存清理时删除。本机目前可复现的替代方案为：

- `C:/Users/junmo/AppData/Roaming/bilibili/ffmpeg/ffmpeg.exe` 虽然版本较旧，但包含 `libass`、`subtitles` 和 `libx264`，能够正确烧录中文字幕。
- `C:/Program Files/Shotcut/ffmpeg.exe` 版本更新，但当前构建不含 `subtitles` 滤镜，不能用于本流程。

更换机器时，优先使用较新的、明确带 `libass` 和 `libx264` 的 FFmpeg 完整构建，不必依赖上述个人路径。

## 5. 按页生成自然配音

### 5.1 讲稿格式

讲稿必须明确标出页码和建议时长，且页数与 PPT 一致。当前两份报告均为 12 页。生成前至少检查：

1. 页码从 1 连续到 12。
2. 每页只有一段对应讲稿，不跨页复用。
3. 讲稿总时长目标为 600～625 秒。
4. 技术缩写、公式读法和中英文混排已经人工确认。
5. 分句后重新拼接的全文必须与原讲稿逐字一致。

### 5.2 不按整页一次性合成

整页一次合成会使语气单调，也难以准确控制重点。当前方法先按句号、问号、分号、冒号及必要的逗号位置拆成语义单元，再针对每个单元设置：

- `rate`：语速；
- `pitch`：音高；
- `volume`：音量；
- `pause_after_seconds`：句后停顿；
- `role`：开场、过渡、技术说明、对比边界、重点结论或收束。

典型策略如下：

- 普通解释保持接近音色的自然语速。
- 公式、缩写和技术边界略微放慢。
- “不是……而是……”“必须”“不能”等对比句适度放慢、提高音量，并增加停顿。
- 开场稍温暖，结尾更坚定，但不使用夸张的播音腔。
- 问句适度抬高音高，结论句在句末保留更长停顿。

### 5.3 两个项目的音色与语气参数

| 项目 | 音色 | 常态语气 | 重点语气 | 当前目标时长 |
|---|---|---|---|---:|
| 声速 | `zh-CN-XiaoxiaoNeural` 女声 | 稍明快、温和，按句动态调整 | 重点句略慢、音高和音量适度提高 | 约 624.7 秒 |
| 李萨如 | `zh-CN-YunyangNeural` 男声 | 语速 `-5%～-8%`、音高 `-3～-5 Hz`、音量 `0～+2%` | 语速 `-10%～-12%`、音高 `-1～+1 Hz`、音量 `+3～+5%` | 约 601.0 秒 |

李萨如音频还进行了以下处理：

- 单个语义单元加入约 14 ms 淡入和淡出；
- 全页使用 `loudnorm=I=-18:LRA=6:TP=-2` 统一响度；
- 页首约 35 ms 淡入、页尾约 60 ms 淡出；
- 检查综合响度、真峰值、削波样本、停顿中点和拼接跳变。

### 5.4 输出格式与时间轴

每页最终保存一段：

```text
WAV PCM，24 kHz，16 bit，单声道
```

同时生成 `segments.json`，每页至少包含：

```json
{
  "slide": 1,
  "audioSeconds": 42.28,
  "displaySeconds": 42.83,
  "startSeconds": 0.0,
  "endSeconds": 42.83,
  "wav": ".../slide-01.wav",
  "voice": "zh-CN-XiaoxiaoNeural",
  "text": "本页完整讲稿"
}
```

其中：

```text
displaySeconds = audioSeconds + 0.55 秒翻页缓冲
```

下一页的 `startSeconds` 等于前面所有页面 `displaySeconds` 之和。SRT 只覆盖实际发音区间，不覆盖句间停顿和页尾缓冲，因此字幕会在说话结束后及时消失。

### 5.5 当前复现脚本

本机当前保留了以下生成脚本：

```text
tmp/narrated_video/generate_sound_expressive.py
tmp/narrated_video/generate_liss_expressive_wav.py
tmp/narrated_video/prepare_narrated_deck.ps1
tmp/narrated_video/export_video.ps1
tmp/narrated_video/render_with_powerpoint.ps1
```

示例运行方式：

```powershell
python ./tmp/narrated_video/generate_sound_expressive.py
python ./tmp/narrated_video/generate_liss_expressive_wav.py
```

注意：根 `.gitignore` 忽略 `tmp/` 和 `**/tmp/`，这些脚本目前只是本机保留的复现参考，并非受 Git 保护的稳定入口。若要长期维护自动化流程，应先把正式脚本迁入受版本控制的目录，再更新本文路径。清理缓存后，中间音频和临时 Python 依赖需要重新生成或安装。

此外，声速生成脚本目前写有本机仓库根路径，PowerPoint 脚本写有 Office16 安装路径；换电脑运行前必须先检查并修改这些配置，不能原样假定路径存在。

## 6. 将逐页音频嵌入 PPT

### 6.1 为什么要嵌入而不是链接

如果 PPT 只链接外部 WAV，移动文件、换机器或导出视频时可能找不到音频。应使用 `Shapes.AddMediaObject2`，设置“链接文件为否、随文档保存为是”，把音频真正写入 PPTX。

每次重新生成前，应先删除名称为 `AutoNarration_*` 的旧音频对象，否则会出现双重配音。

### 6.2 每页设置

核心逻辑如下：

```powershell
$audioShape = $slide.Shapes.AddMediaObject2(
    $wavPath,
    0,   # 不链接外部文件
    -1,  # 嵌入并随文档保存
    -48, -48, 16, 16
)

$audioShape.Name = ('AutoNarration_{0:00}' -f $slideNumber)
$audioShape.Left = -1000
$audioShape.Top = -1000
$audioShape.Width = 1
$audioShape.Height = 1

$play = $audioShape.AnimationSettings.PlaySettings
$play.PlayOnEntry = -1
$play.HideWhileNotPlaying = -1
$play.LoopUntilStopped = 0
$play.PauseAnimation = 0
$play.StopAfterSlides = 1

$transition = $slide.SlideShowTransition
$transition.AdvanceOnClick = 0
$transition.AdvanceOnTime = -1
$transition.AdvanceTime = [single]$displaySeconds
```

音频图标被移到画布外，只隐藏图标，不影响嵌入和播放。

### 6.3 示例命令

声速：

```powershell
./tmp/narrated_video/prepare_narrated_deck.ps1 `
  -SourcePptx './声速/设计报告/声速测量实验智能助教_答辩PPT.pptx' `
  -SegmentsJson './tmp/narrated_video/sound_expressive/segments.json' `
  -OutputPptx './声速/设计报告/声速测量实验智能助教_自动配音版.pptx'
```

李萨如：

```powershell
./tmp/narrated_video/prepare_narrated_deck.ps1 `
  -SourcePptx './李萨如/设计报告/李萨如图形实验智能助教_答辩PPT.pptx' `
  -SegmentsJson './tmp/narrated_video/liss_expressive/segments.json' `
  -OutputPptx './李萨如/设计报告/李萨如图形实验智能助教_自动配音版.pptx'
```

生成后必须重新打开 PPT，抽查第一页、中间页和最后一页是否自动播放、是否按时翻页，并确认页面上没有可见的音频图标。

## 7. 使用 Microsoft PowerPoint 导出视频

### 7.1 防止误连到 WPS

当前机器的 `PowerPoint.Application` COM 注册曾指向 WPS。WPS 对象虽然暴露 `AddMediaObject2`、`CreateVideo` 和 `CreateVideoStatus`，但实测调用后状态一直停留在 `0`，没有生成 MP4。

可靠做法是显式启动：

```text
C:/Program Files/Microsoft Office/Root/Office16/POWERPNT.EXE
```

随后绑定活动 COM 对象，并检查：

```powershell
if ($application.Path -ne 'C:\Program Files\Microsoft Office\Root\Office16') {
    throw "绑定到的不是 Microsoft PowerPoint"
}
```

如果所用 PowerShell/.NET 环境没有 `Marshal.GetActiveObject()`，可改用 `oleaut32.dll!GetActiveObject` 的 P/Invoke；无论采用哪种绑定方式，都必须核对应用路径，不能只相信 ProgID。

### 7.2 导出参数

当前视频使用：

```powershell
$presentation.CreateVideo(
    $outputMp4,
    $true, # 使用PPT中已经设置的计时和旁白
    5,     # 未设置计时页面的默认秒数
    1080,  # 垂直分辨率
    30,    # 帧率
    85     # PowerPoint质量参数
)
```

`CreateVideoStatus` 状态含义：

| 值 | 含义 |
|---:|---|
| 0 | 尚未开始或无任务 |
| 1 | 正在处理 |
| 2 | 已排队 |
| 3 | 完成 |
| 4 | 失败 |

必须保持 PowerPoint 和 Presentation 对象存活，持续轮询到状态 `3` 后才能关闭。十分钟 1080p 视频建议把超时设置为 30～60 分钟。

### 7.3 示例命令

```powershell
./tmp/narrated_video/export_video.ps1 `
  -NarratedPptx './声速/设计报告/声速测量实验智能助教_自动配音版.pptx' `
  -OutputMp4 './声速/设计报告/声速测量实验智能助教_无字幕临时视频.mp4' `
  -VerticalResolution 1080 `
  -FramesPerSecond 30 `
  -Quality 85 `
  -TimeoutMinutes 60
```

李萨如只需替换输入和输出路径。

不要直接覆盖唯一正式视频。先输出到临时文件，完成时长、画面和音轨检查后，再将其作为字幕烧录的输入。

## 8. 生成与整理 SRT

### 8.1 正式 SRT 与烧录 SRT 分开

语音生成阶段输出的正式 SRT 以完整语义句为单位，便于校对、检索和以后重新编辑。当前正式文件为：

| 项目 | 正式 SRT 条数 | 特点 |
|---|---:|---|
| 声速 | 98 | 单条最长约 60 个可见字符 |
| 李萨如 | 73 | 部分条目包含 2～3 行排版 |

直接烧录时，长字幕会占据两三行并覆盖 PPT 底部内容。因此烧录前创建临时副本并细分：

| 项目 | 烧录用临时条数 | 单条最长字符数 |
|---|---:|---:|
| 声速 | 133 | 31 |
| 李萨如 | 112 | 30 |

正式 SRT 不替换、不删除。临时字幕只服务于硬字幕视频。

### 8.2 长字幕细分规则

1. 优先在句号、分号、冒号、逗号等自然标点处分割。
2. 如果有多个候选分割点，选择最接近可见字符中点的位置。
3. 技术词组不可拆开，例如 `Python`、`Julia`、`WGLMakie`、`Streamlit`、`SVD` 和“确定性复算”。
4. 新条目的时间区间必须完全落在原条目区间内，保持首尾时间不变。
5. 可按两段可见字符数量分配原时长，并为过短片段设置最小可读时长。
6. 拆分后按顺序拼接全部字幕文字，必须与拆分前完全一致。
7. 所有区间必须满足 `start < end`，相邻字幕不得倒序或重叠。

不要通过删除讲稿内容来缩短字幕；应通过增加条目数量改善可读性。

## 9. 使用 FFmpeg 烧录硬字幕

### 9.1 字幕样式

当前采用的基础样式为微软雅黑、白字、约 50% 透明黑色底板和底部居中。两份视频的最终样式并非完全相同：

| 项目 | 关键样式 | 编码预设 | 原因 |
|---|---|---|---|
| 声速 | `FontSize=17`、`BorderStyle=3`、`Outline=1`、`MarginV=7`、黑色半透明底板与描边 | `veryfast` | 字幕已经拆成单行；较低位置能避开正文并保留页码 |
| 李萨如 | `FontSize=17`、`BorderStyle=3`、`Outline=0`、`MarginV=30`、黑色半透明底板 | `medium` | 需要避开封面左下角栏目和末页底部小字 |

在最终烧录环境中，`FontSize=17` 在 1080p 画面上的视觉高度约为 40 px。

该数值不是通用标准。更换模板、字体或 FFmpeg 版本后，必须重新抽帧检查。

### 9.2 中文路径的稳妥处理

旧版 `subtitles` 滤镜处理 Windows 盘符、反斜杠和中文路径时容易出现转义问题。最稳妥的方法是：

1. 把烧录用 SRT 复制到纯英文临时目录；
2. 命名为 `captions.srt`；
3. 进入该目录；
4. 滤镜中只使用相对路径 `captions.srt`。

### 9.3 PowerShell 示例

以下示例生成声速字幕版；运行前应把 `$inputVideo` 指向无字幕的 PowerPoint 导出视频，并保证 `$outputVideo` 不是同一个文件：

```powershell
$ffmpegExe = 'C:\Users\junmo\AppData\Roaming\bilibili\ffmpeg\ffmpeg.exe'
$inputVideo = (Resolve-Path './声速/设计报告/声速测量实验智能助教_无字幕临时视频.mp4').Path
$outputVideo = Join-Path (Resolve-Path './声速/设计报告').Path '声速测量实验智能助教_字幕候选.mp4'
$sourceSrt = (Resolve-Path './tmp/subtitle_work/sound_split.srt').Path
$subtitleWork = Join-Path ([IO.Path]::GetTempPath()) 'renai_sound_hardsub'

New-Item -ItemType Directory -Force -Path $subtitleWork | Out-Null
Copy-Item -LiteralPath $sourceSrt -Destination (Join-Path $subtitleWork 'captions.srt') -Force

$style = 'FontName=Microsoft YaHei,FontSize=17,PrimaryColour=&H00FFFFFF,OutlineColour=&H80000000,BackColour=&H80000000,BorderStyle=3,Outline=1,Shadow=0,Alignment=2,MarginV=7'
$filter = "subtitles=filename='captions.srt':force_style='$style'"

Push-Location $subtitleWork
try {
    & $ffmpegExe -hide_banner -y `
      -i $inputVideo `
      -vf $filter `
      -map 0:v:0 -map 0:a:0 `
      -c:v libx264 -preset veryfast -crf 18 `
      -pix_fmt yuv420p `
      -c:a copy `
      -movflags +faststart `
      $outputVideo
    if ($LASTEXITCODE -ne 0) { throw "FFmpeg字幕烧录失败：$LASTEXITCODE" }
}
finally {
    Pop-Location
}
```

李萨如最终使用的滤镜和映射略有不同：

```powershell
$style = 'FontName=Microsoft YaHei,FontSize=17,PrimaryColour=&H00FFFFFF,BackColour=&H80000000,BorderStyle=3,Outline=0,Shadow=0,Alignment=2,MarginV=30'
$filter = "subtitles=filename='captions.srt':charenc=UTF-8:original_size=1920x1080:force_style='$style'"

& $ffmpegExe -hide_banner -y `
  -i $inputVideo `
  -vf $filter `
  -map 0:v:0 -map '0:a:0?' `
  -c:v libx264 -preset medium -crf 18 `
  -pix_fmt yuv420p -c:a copy -movflags +faststart `
  $outputVideo
```

以上是最终成片的实际参数。若统一改用更新的 FFmpeg 构建，字体尺寸和边距仍需重新抽帧验证。

### 9.4 为什么添加字幕后文件反而更小

硬字幕要求重新编码视频画面。当前命令采用 `libx264 -crf 18`，而 PowerPoint 导出的 PPT 视频画面码率较高。PPT 大部分时间是静止画面，H.264 能大量复用前后帧，因此重新编码后画面码率从约 `0.9～1.0 Mbps` 降到约 `0.17～0.18 Mbps`；AAC 音轨仍由 `-c:a copy` 原样保留。

当前结果：

| 项目 | 实际烧录输入视频 | 最终硬字幕视频 | 体积减少 |
|---|---:|---:|---:|
| 声速 | 99.67 MiB | 37.00 MiB | 62.9% |
| 李萨如 | 89.02 MiB | 36.56 MiB | 58.9% |

这里采用的是字幕烧录时实际输入文件的大小。Git 中更早一次提交的无字幕视频分别为 96.35 MiB 和 89.91 MiB，因此若以 Git 历史版本为基准，会得到 61.6% 和 59.3% 的下降比例；两套口径不同是因为烧录前曾重新生成过源视频。

体积变小并不是因为分辨率或时长降低；两份视频仍是 1920×1080，音轨也没有被删除。CRF 18 对静态幻灯片通常接近视觉无损，但仍必须抽帧检查公式、细线和小字号文字。

## 10. 验收清单

### 10.1 配音与时间轴

- [ ] 恰好生成 12 段逐页 WAV。
- [ ] WAV 均为 24 kHz、16 bit、单声道。
- [ ] 分句后的文字重新拼接与源讲稿逐字一致。
- [ ] 总展示时长在 600～625 秒。
- [ ] 每页都保留约 0.55 秒翻页缓冲。
- [ ] 没有削波、爆音、异常长静音或明显响度跳变。
- [ ] 男声与女声没有混用。

### 10.2 自动配音 PPT

- [ ] PPT 页数与 WAV 数量相同。
- [ ] 每页只有一个 `AutoNarration_*` 音频对象。
- [ ] 音频已嵌入而不是外部链接。
- [ ] 进入页面后自动播放，不循环，不跨页继续播放。
- [ ] 翻页时间大于本页实际音频时长。
- [ ] 音频图标位于画布外，页面没有新增可见元素。

### 10.3 PowerPoint 导出视频

- [ ] `CreateVideoStatus` 最终为 `3`。
- [ ] 输出文件存在且大小合理。
- [ ] 分辨率为 1920×1080，画面编码为 H.264。
- [ ] 存在 AAC 音轨，采样率和声道数不为零。
- [ ] 视频总时长与 `segments.json` 总展示时长误差不超过约 3 秒。
- [ ] 第一页、中间页、最后一页均没有截断尾句。

### 10.4 字幕

- [ ] SRT 使用 UTF-8 或 UTF-8 BOM。
- [ ] 条目编号连续，时间非负，`start < end`，没有重叠。
- [ ] 临时细分字幕重新拼接后与正式 SRT 文字一致。
- [ ] 首条、末条、最长条目和技术词条均人工检查。
- [ ] 抽查开头、25%、50%、75%和结尾画面。
- [ ] 字幕不裁切，不遮挡页码、封面栏目、公式或结论文字。
- [ ] 中文字体实际生效，没有回退成方框或乱码。

### 10.5 硬字幕视频

- [ ] 视频仍为 1920×1080、H.264、约 30 fps。
- [ ] 使用 `-pix_fmt yuv420p`，保证常见播放器兼容性。
- [ ] 使用 `-movflags +faststart`，便于网络或移动设备边下边播。
- [ ] 从烧录前后视频分别抽取原始 AAC，音轨哈希一致。
- [ ] 最终视频计算并记录 SHA-256。
- [ ] 只有全部检查通过后，才替换正式文件。

## 11. 常见问题

### 11.1 `PowerPoint.Application` 实际打开了 WPS

原因是 COM ProgID 被 WPS 注册。应显式启动 Microsoft `POWERPNT.EXE` 并检查 `Application.Path`，不要仅使用 `New-Object -ComObject PowerPoint.Application`。

### 11.2 `CreateVideo()` 没报错，但没有生成文件

可能绑定到了 WPS，或 PowerPoint 对象在导出完成前被释放。必须轮询 `CreateVideoStatus`，直到状态为 `3` 再关闭演示文稿。

### 11.3 每页尾句被截断

`AdvanceTime` 小于音频真实时长。不要使用讲稿的估计秒数直接翻页，应读取生成后 WAV 的真实时长，并增加约 0.55～0.60 秒缓冲。

### 11.4 出现双重配音

同一页重复嵌入了音频。重新生成前删除旧的 `AutoNarration_*` 媒体对象。

### 11.5 FFmpeg 提示没有 `subtitles` 滤镜

当前 FFmpeg 构建缺少 `libass`。更换完整构建；只升级版本号不能保证具备字幕滤镜。

### 11.6 中文字幕路径解析失败

把 SRT 复制到纯英文临时目录，改名为 `captions.srt`，进入目录后使用相对路径。

### 11.7 字幕遮挡 PPT 底部内容

先把长字幕按语义拆成单行，再调整 `MarginV`；不要只把三行字幕整体上移，因为这样可能遮挡正文。不同 PPT 模板需要分别抽帧验证。

### 11.8 视频加字幕后明显变小

这是 `libx264 -crf 18` 对静态 PPT 的高效重编码结果，不代表音轨被删除。应检查分辨率、时长、音轨、关键帧清晰度和 AAC 哈希，而不是用文件大小单独判断质量。

## 12. 归档与清理建议

正式保留：

- 原始答辩 PPT；
- 逐页讲稿；
- 自动配音版 PPT；
- 正式 SRT；
- 最终硬字幕 MP4；
- 音色、版本、时长和哈希记录；
- 受版本控制的生成脚本。

可以在验收后清理：

- 逐句 MP3；
- 逐句和逐页临时 WAV；
- 临时静音片段；
- 临时细分 SRT；
- 无字幕 PowerPoint 导出候选；
- FFmpeg 烧录候选；
- 音轨抽取文件；
- 抽帧截图和 QA 渲染图；
- 临时安装的 Python 依赖副本。

清理前必须先确认正式 PPT、SRT 和 MP4 已复制到两个项目的 `设计报告/` 目录，并对正式文件计算哈希。`tmp/` 下的生成物均应视为可重建缓存，不能把唯一正式成果只保存在其中。
